[defaults]
inventory = inventory
sudo_user       = root
host_key_checking = False
# roles_path    = $HOME/.ansible/roles

[privilege_escalation]
become          =True
become_method   =sudo
become_user     =root
become_ask_pass =False